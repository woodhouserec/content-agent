{
  "name": "content-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrations:local": "wrangler d1 migrations apply content-agent --local",
    "db:migrations:remote": "wrangler d1 migrations apply content-agent --remote"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
